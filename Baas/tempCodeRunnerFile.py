    # command = "git clone https://github.com/alexcasalboni/aws-lambda-power-tuning.git"
    # output = subprocess.run(command, shell=True, capture_output=True, text=True)
    # print(output.stdout)
    # print(output.stderr)